import { redirect } from 'next/navigation';

export default function CalendarPage() {
  // Rediriger vers la page de récompenses où le calendrier est maintenant intégré
  redirect('/recompenses');
}

